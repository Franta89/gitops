.DS_Store
*.swp

# Plaintext secret files — all secrets now live in Azure Key Vault.
# These entries prevent accidental commit if a plaintext file is created locally.
manifests/cert-manager/cloudflare-api-token-secret.yaml
manifests/news-digest/config/newsapi-secret.yaml
manifests/news-digest/postgres/secret.yaml
pub-cert.pem
