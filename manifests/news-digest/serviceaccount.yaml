# After running `terraform apply` in infra-terraform, replace the placeholder
# below with: terraform output managed_identity_client_id
apiVersion: v1
kind: ServiceAccount
metadata:
  name: news-digest-sa
  namespace: news-digest
  annotations:
    azure.workload.identity/client-id: "REPLACE_WITH_TERRAFORM_OUTPUT_managed_identity_client_id"
